# Rust build output. One `target/` at the workspace root, shared by every crate.
target/

# Python bytecode
__pycache__/
*.py[cod]

# Screenshots written by the F12 shortcut, which saves next to the binary
iris3d-*.png

# The uv virtualenv ignores itself via .venv/.gitignore.
