[package]
name = "iris3d"
version = "0.1.0"
edition = "2024"

# Wiring only. This crate adds the plugins in order and parses the command line;
# everything it does is in the crates below.
[dependencies]
iris3d-core = { workspace = true }
iris3d-draw = { workspace = true }
iris3d-filter = { workspace = true }
iris3d-grpc = { workspace = true }
iris3d-scene = { workspace = true }
iris3d-view = { workspace = true }

bevy = { workspace = true }
clap = { workspace = true }
